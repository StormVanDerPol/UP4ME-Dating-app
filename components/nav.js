import {
    StyleSheet, Text,
} from 'react-native';

const Nav = ({ navigation }) => {
    return (
        <>
            <Text>
                I WORK
            </Text>
        </>
    );
}

const s = StyleSheet.create({

});

export default Nav;